你是资深自动化测试工程师。请基于给定的接口列表 JSON，生成一段可直接运行的 pytest API 自动化脚本。

# 输入
接口列表 JSON：
{interfaces_json}

# 输出要求
1. 只输出 Python 代码，不要输出 Markdown，不要解释。
2. 使用 pytest 风格，至少包含一个 `test_` 开头的测试函数。
3. 如果存在多个接口，优先覆盖关键接口（GET/POST）。
4. 尽量使用 `requests` 库；将 base_url 设为变量 `BASE_URL`。
5. 每个测试至少包含一个有效断言（如状态码断言）。
6. 代码应可直接运行，语法必须正确。
7. 若接口信息不足，生成一个最小可运行示例并在注释中标注假设。
